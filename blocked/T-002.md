# T-002: 専用 CLI の公式ログイン待ち

2026-09-08。T-001 は Done。専用 HOME の公式ログインを開始し、ブラウザーでの本人認証を待っている。Spark の正常呼出しは未実施。

- `probe_cli.py login-live` は保存済み offline 証跡の確認後、専用 HOME の `codex login status` が未ログインであることを確認し、公式 `codex login` を開始した。
- 公式出力: `dedicated_login_status=1`、`Starting local login server on http://localhost:1455.`。Chrome に OpenAI のログイン画面を開いた。
- この出力と画面の観測を `evidence/live/login-state.json` に記録した。OAuth URL と認証情報は記録していない。
- live 生成数: 0。`live-home/`、`config/live.toml`、`evidence/live/` は用意済み。既存ユーザーの `auth.json` は読取り・転用していない。
- `metadata-live`、`run-live`、`verify-live` は実装準備済み・未実行。利用枠と credit 条件の当日確認も未実施。`receipt.json` は未作成。
- 実呼出しは公式ログイン後の ChatGPT Pro / Spark の model metadata / quota / credit を確認し、60秒・一回限りで実施する。実行直前に排他的に receipt を作成して fsync し、以後の生成操作は拒否する。verify は証跡を読むだけ。
- ログインの完了が必要。認証待ちが終了・失効した場合は `login-live` を再開する。ログインのやり直しは生成を伴わない。
- 正常応答の検証は接続実証用の narrative JSON と公開入力の境界を対象とする。既存 Gateway の Semantic Result 適用、subscription budget 永続化、アプリ adapter は別の実装範囲。

実証資材・証跡・認証領域は専用一時 root に保持し、git へ追加しない。実証完了後の認証領域の片付けは未実施。Real Provider 導入と Public Context は未完了。
