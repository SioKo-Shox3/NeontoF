# T-001: offline 実証の隔離環境が未成立

2026-09-08。旧停止記録。隔離条件の決定を実施側に委ねるユーザー指示により再開し、T-001 は Done。以下の停止理由は OS 全通信遮断を要件にしていた時点の経緯。

## 再開結果

専用 HOME・人工認証・人工公開入力・継承環境変数の allowlist を使用し、生成・認証更新・設定取得先を localhost に固定した。外部宛てを転送しない proxy の拒否応答も確認した。OS Firewall は変更していない。これは固定版 CLI の設定による通信先の制御であり、OS レベルの全通信遮断を保証しない。

```text
probe_cli.py verify-offline
normal: verified HTTP=1, refresh=0, tools=0
auth_recovery: verified HTTP=3, refresh=1, tools=0
auth_exhausted: verified HTTP=3, refresh=1, tools=0
timeout: verified HTTP=1, refresh=0, tools=0
server_error: verified HTTP=1, refresh=0, tools=0
disconnect: verified HTTP=1, refresh=0, tools=0
verify-offline: PASS (configured routing, not OS packet containment)
```

exit 0。`timeout` は SSE idle timeout、`server_error` は HTTP503、`disconnect` は SSE 成立後の切断。timeout 検証の idle 設定は500ms。呼出し全体の12秒制限では打ち切られていない。live は60秒で別途確認する。

各ケースの `result.json`、`requests.json`、`config.toml`、`stdout.jsonl`、`stderr.txt`、`trace/` を一時 root の `evidence/offline/<case>/` に保存した。検証は保存済みの証拠を読み、HTTP 受信内容と trace payload の完全一致・回数・正常完了数・設定と入力の hash を検査する。モデルを再呼出ししない。

公開入力は既存 `ProviderRequest` の検証に通る全フィールドを含み、Dice は公開4フィールドだけ。最終 tools は空。初回の探索では質問用 Tool が残っていたため `tools.experimental_request_user_input.enabled=false` で解消し、初回証拠は `normal-discovery/` に保持した。起動時の `/api/codex/settings/user` は localhost の設定取得として別記録する。

版固定公式 source の参照箇所と hash は `evidence/offline/source-contract.json`。CLI 接続の実証であり、Semantic Result の適用・アプリ adapter・実 Provider の費用処理は含まない。

## 停止理由

実証には外部通信を遮断した状態で localhost の人工 HTTP receiver に接続する必要がある。既存の offline sandbox 用 Firewall 規則は、外部通信と localhost の TCP 全ポートを遮断する構成だった。公式 source では offline sandbox SID に適用される規則であり、通常のユーザープロセス全体の通信遮断を示すものではない。許可された proxy port は空で、現在のプロセスは管理者ではない。

この構成から検証 receiver を許可するには sandbox の provision 設定と Firewall 規則の変更が関係する。既存 OS 設定の変更は実証用一時領域の承認に含まれていない。HTTP proxy 環境変数だけでは外部遮断の根拠にできないため、隔離成立前の CLI 起動を止めた。

これは構成確認による停止判断であり、通信遮断の実測結果でも、他の隔離方法すべてが不可能という結論でもない。

## 確認コマンドと取得値

```powershell
& 'C:\Users\KINGkawamura\.codex\packages\standalone\releases\0.153.3-x86_64-pc-windows-msvc\bin\codex.exe' --version
```

```text
codex-cli 0.153.3
```

```powershell
$marker = Get-Content -Raw -LiteralPath 'C:\Users\KINGkawamura\.codex\.sandbox\setup_marker.json' | ConvertFrom-Json
[pscustomobject]@{version=$marker.version; proxy_ports=$marker.proxy_ports; allow_local_binding=$marker.allow_local_binding} | ConvertTo-Json -Compress
$identity = [Security.Principal.WindowsIdentity]::GetCurrent()
$principal = [Security.Principal.WindowsPrincipal]::new($identity)
'IsAdministrator=' + $principal.IsInRole([Security.Principal.WindowsBuiltInRole]::Administrator)
```

```text
{"version":5,"proxy_ports":[],"allow_local_binding":false}
IsAdministrator=False
```

`Get-NetFirewallRule -PolicyStore ActiveStore -DisplayName 'codex_sandbox_offline*'` と各規則の `Get-NetFirewallAddressFilter` / `Get-NetFirewallPortFilter` の出力から、下表の構成値を抽出して保存した。三規則はいずれも `enabled=True`、`direction=Outbound`、`action=Block`。

| 規則 | protocol | remote port | remote address |
|---|---|---|---|
| codex_sandbox_offline_block_loopback_tcp | TCP | 1-65535 | 127.0.0.0/255.0.0.0, ::/127 |
| codex_sandbox_offline_block_loopback_udp | UDP | Any | 127.0.0.0/255.0.0.0, ::/127 |
| codex_sandbox_offline_block_outbound | Any | Any | 0.0.0.0-126.255.255.255, 128.0.0.0-255.255.255.255, ::, ::2-ffff:ffff:ffff:ffff:ffff:ffff:ffff:ffff |

公式 version 固定 source の [firewall.rs](https://github.com/openai/codex/blob/rust-v0.153.3/codex-rs/windows-sandbox-rs/src/bin/setup_main/win/firewall.rs#L88) では、`allow_local_binding` と `proxy_ports` に応じて loopback 遮断規則を変更する。`proxy_ports=[]` に対する TCP 遮断範囲は全ポートとなり、取得した設定と整合する。

証跡は `C:\Users\KINGkawamura\AppData\Local\Temp\NeontoF-provider-proof\codex-0.153.3\evidence\offline\network-preflight.json`。取得時刻、実行コマンドの説明、抽出した構成値、公式 source の該当箇所を含み、内容を開いて確認した。公開 git へは追加しない。

## 未実施と再開条件

- `probe_cli.py` は未作成。不存在の `verify-offline` は実行していない。
- tools、入力、401、timeout、5xx、切断の実 payload / 受信数検証は未実施。offline は不合格扱いであり、Done ではない。
- 認証情報の読取り・転用、専用ログイン、Spark 生成、Firewall 変更は行っていない。
- 再開には、外部遮断と人工 receiver への到達性を両立する隔離構成が必要。既存 sandbox を変更する案なら、対象設定・権限・他セッションへの影響・復元手順を具体化してから実施範囲を確定する。
- 公式 source にある候補は、`proxy_ports` に受信用 TCP ポートを登録する方法と、`allow_local_binding=true` で loopback 遮断規則を除去する方法。前者は許可先を TCP ポートで絞り、UDP の遮断を維持する。後者は TCP/UDP 双方の loopback 遮断規則を除去する。いずれも管理者権限での provision 変更を含めた影響確認と、変更後・復元後の通信実測が必要であり、今回の構成確認だけでは採用を確定できない。

一時領域の構成確認証跡は再開時の比較用に保持する。認証領域は作成していないため認証情報の片付けは発生していない。
